#This shell script should build the docker image for the release and tag it appropriately, then use Tauri to build the MSI installer and write it to the /dist folder
echo "This script should be building the tauri installer(s), but for now it doesn't do that"